# 입낚 프로젝트 전체 점검 보고서

- **점검일**: 2026-08-11
- **브랜치**: `main` (HEAD `70c7879`)
- **범위**: 코드 / 기능 / UI·UX / 성능 (코드 수정 없음, 분석·보고만)
- **규모**: TS/TSX 495개 파일, 약 75,900줄 + `src/services/*.js` 7개 파일

---

## 0. 요약

| 항목 | 결과 |
|---|---|
| `npx tsc --noEmit` | ✅ **통과** (에러 0, 경고 0) |
| `npm run build` | ✅ **통과** (exit 0, 빌드 에러 0) |
| `npx next lint` | ⚠️ **통과 / 경고 9건** (모두 `react-hooks/exhaustive-deps`) |
| 관리자 API 인가 | ✅ 전 라우트 인증 게이트 존재 |
| 소유권(IDOR) 검증 | ✅ market·posts·fishing-spots 모두 본인 확인 |
| XSS 벡터 | ✅ 사용자 데이터 `dangerouslySetInnerHTML` 없음 |
| 경로 탐색(Path traversal) | ✅ uploads·models·ai-training 모두 차단 |
| **발견 이슈** | **높음 6건 / 중간 12건 / 낮음 7건** |

빌드·타입 체크는 깨끗합니다. 문제는 대부분 **런타임 데이터 무결성**과 **계측일지 저장 아키텍처**에 몰려 있습니다.
특히 아래 3건은 실사용자 데이터가 소실되거나 잘못 표시되는 문제라 우선 처리를 권합니다.

1. 계측일지가 localStorage 전용이라 서버 백업이 전혀 없음 (H-1)
2. 계측일지가 회원별로 분리되지 않아 같은 기기의 다른 계정에 노출 (H-2)
3. 사진 업로드 실패 시 무작위 스톡 이미지가 내 물고기 사진으로 저장 (H-4)

---

## 1. 빌드 / 타입 점검 상세

### 1.1 TypeScript — 통과
```
npx tsc --noEmit  →  exit 0, 출력 없음
```
`strict: true` 상태에서 에러 0건입니다.

다만 타입 안전성이 실제로 미치는 범위는 보이는 것보다 좁습니다 (L-3, L-4 참고).

### 1.2 Next.js 빌드 — 통과
```
npm run build  →  exit 0
```
- 빌드 에러·경고 없음
- 공유 First Load JS **88.3 kB** — 양호한 수준
- 가장 무거운 페이지: `/measure` 40.9 kB (185 kB), `/me` 17.9 kB (157 kB), `/map` 16.8 kB (156 kB), `/groups/[id]` 18.1 kB (138 kB)
- Middleware 26.7 kB
- `next.config.mjs`의 `optimizePackageImports: ["lucide-react","date-fns"]`, AVIF/WebP, `minimumCacheTTL` 7일 설정은 적절합니다.

### 1.3 ESLint — 경고 9건

| 파일 | 위치 | 내용 |
|---|---|---|
| `src/app/groups/page.tsx` | 50:32 | `useEffect` 누락 의존성 `load` |
| `src/app/groups/[id]/manage/page.tsx` | 91:32 | `useEffect` 누락 의존성 `load` |
| `src/app/me/shipping/page.tsx` | 51:42 | `useEffect` 누락 의존성 `fetchAddresses` |
| `src/components/admin/AdminIpnakBallOrdersTab.tsx` | 53:39 | `useEffect` 누락 의존성 `fetchOrders` |
| `src/components/AiPointRecommend.tsx` | 106:9 (×2) | `points` 논리식이 매 렌더 `useMemo` 의존성 변경 |
| `src/components/LiveScanCamera.tsx` | 760:26 | cleanup에서 `videoRef.current` 참조 (변경됐을 수 있음) |
| `src/components/LiveScanCamera.tsx` | 1081:6 | `useEffect` 누락 의존성 `refType`, `testBall` |
| `src/components/TripShareActions.tsx` | 164:6 | `useCallback` 불필요 의존성 `description` |

전부 빌드를 막지는 않지만, **stale closure 잠재 버그**입니다.
특히 `LiveScanCamera.tsx:1081`은 스캔 폴링 이펙트가 `refType`(볼/키링)을 의존성에서 빠뜨리고 있어,
측정 중 기준물 종류를 바꿔도 폴링 콜백은 옛 `refType`으로 계속 요청할 수 있습니다.

---

## 2. 심각도 **높음** (6건)

### H-1. 계측일지가 localStorage 전용 — 서버 백업이 전혀 없음 ⚠️ 데이터 유실

**파일**: `src/services/DatabaseService.js`, `src/services/SyncService.js:19`, `src/services/MeasurementApiService.js:6`

계측일지(`DiarySheet`, `/diary`)의 유일한 저장소가 브라우저 localStorage입니다.

```js
// DatabaseService.js:8
const STORE_KEY = 'ipnak_measurements'   // localStorage 전용

// SyncService.js:19
if (measurementApi.isMock) return        // ← 여기서 항상 조기 반환

// MeasurementApiService.js:6
const USE_MOCK = process.env.NEXT_PUBLIC_USE_MOCK_API !== 'false'
get isMock() { return USE_MOCK || !BASE }
```

`.env`에 `NEXT_PUBLIC_USE_MOCK_API`도 `NEXT_PUBLIC_API_BASE_URL`도 **정의되어 있지 않습니다**.
따라서 `isMock`은 항상 `true`이고, `syncPendingMeasurements()`는 **단 한 번도 서버로 업로드하지 않습니다.**
`measure/page.tsx:551`의 `syncService.syncPendingMeasurements()` 호출도 즉시 반환됩니다.

**영향**
- 브라우저 데이터 삭제 / 시크릿 모드 종료 / 앱 재설치 → 계측일지 전량 소실
- 기기 변경 시 이전 기록 이전 불가
- iOS Safari ITP는 7일 미접속 시 스크립트 기록 저장소를 삭제 → **아이폰 사용자는 2주만 안 써도 일지가 사라질 수 있음**

**참고**: 측정 시 `/api/catch`로 서버에도 별도 저장되긴 하지만, 이는 `CatchRecord`/`FishingPoint`이며
계측일지 화면(`DiarySheet`)은 `dbService`만 읽습니다. 즉 **일지 UI가 보는 데이터에는 서버 백업이 없습니다.**

---

### H-2. 계측일지가 회원별로 분리되지 않음 ⚠️ 개인정보 노출

**파일**: `src/services/DatabaseService.js:8`, `src/app/api/auth/logout/route.ts`

```js
const STORE_KEY = 'ipnak_measurements'   // userId 없음 — 기기 전역 단일 키
```

전체 코드베이스에서 `ipnak_measurements`를 지우는 곳이 **한 군데도 없습니다**.
로그아웃 라우트는 서버 세션과 쿠키만 정리하고 localStorage는 건드리지 않습니다.

```ts
// logout/route.ts — 전문
export async function POST() {
  await destroySession();           // 쿠키 + Session 행만 삭제
  return NextResponse.json({ ok: true });
}
```

**영향**: 같은 기기(공용 PC, 가족 공유 폰, 매장 시연기)에서 A가 로그아웃하고 B가 로그인하면
**B가 A의 계측일지 전체(사진·GPS 위치·날짜 포함)를 그대로 봅니다.** 위치정보가 포함되어 있어 민감도가 높습니다.

---

### H-3. 전화번호 인증 1회로 무제한 계정 생성 가능

**파일**: `src/app/api/auth/signup/route.ts:44-51`

```ts
const verified = await prisma.phoneVerification.findFirst({
  where: { phone: cleanPhone, verified: true },   // 만료 검사 없음
  orderBy: { createdAt: "desc" },
});
if (!verified) return ... "phone-not-verified"
```

세 가지 문제가 겹쳐 있습니다.

1. **만료 검사 없음** — `verify-otp`에서는 `expiresAt: { gte: new Date() }`를 확인하지만, 가입 단계에서는 `verified: true`만 봅니다.
2. **인증 레코드를 소모하지 않음** — 가입 성공 후 `PhoneVerification` 행을 삭제/무효화하지 않습니다.
3. **`User.phone`에 `@unique` 없음** (`prisma/schema.prisma:23` — `phone String?`)

**영향**: 한 번 SMS 인증에 성공한 번호는 **영구적으로 무제한 가입에 재사용**됩니다.
가입 축하 알림톡 비용, 가입 리워드/포인트 어뷰징, 대회 중복 참가에 노출됩니다.

---

### H-4. 사진 업로드 실패 시 무작위 스톡 이미지가 내 물고기 사진이 됨

**파일**: `src/app/api/catch/route.ts:32`, `src/app/measure/page.tsx:546-558`

```ts
// catch/route.ts:32
const photo = b.photoUrl || `https://picsum.photos/seed/mycatch-${Date.now()}/800/800`;
```

`measure/page.tsx`의 저장 흐름은 업로드 실패를 **조용히 삼킵니다**:

```ts
try {
  const up = await fetch("/api/upload", { method: "POST", body: form });
  if (up.ok) { ...uploadedPhotoUrl = upData.url ?? null; }
} catch { /* 업로드 실패 시 photoUrl null 로 저장 */ }
```

`up.ok`가 false여도(413, 429 일일 50회 한도 초과, 500) `uploadedPhotoUrl`은 null로 남고,
서버는 그 자리에 **picsum.photos의 랜덤 이미지**를 넣습니다.

**영향**: 지하철/터널 등 약전계에서 측정 후 저장하면, 피드·내 기록·포인트 상세에
**전혀 관계없는 남의 사진(강아지·풍경 등)이 "내가 잡은 물고기"로 표시**됩니다.
운영 데이터에 외부 CDN 링크가 섞여 들어가고, picsum이 내려가면 깨진 이미지가 됩니다.

---

### H-5. GPS 없을 때 좌표가 "서울 한강"으로 자동 설정

**파일**: `src/app/api/catch/route.ts:33`

```ts
const lat = b.lat ?? 37.5326, lng = b.lng ?? 126.9905;   // = taxonomy.ts 의 "한강"
```

이 좌표로 `FishingPoint`가 **실제로 생성**됩니다 (`tx.fishingPoint.create`).

**영향**
- 위치 권한을 거부했거나 실내에서 측정한 모든 기록이 한강에 마커로 찍힘
- `MapScreen`의 "내 포인트", `myRegions` 지역별 집계, AI 포인트 추천(`/api/points/recommend`)이 전부 오염
- 사용자 입장에서는 "가지도 않은 한강에 내 조과가 찍혀 있음"

좌표가 없으면 `FishingPoint`를 만들지 않거나 nullable 처리하는 편이 맞습니다.

---

### H-6. localStorage 용량 초과 시 사진을 조용히 삭제

**파일**: `src/services/DatabaseService.js:60-72`

```js
try { this._lsSet(STORE_KEY, list) }
catch (e) {
  item.imageBase64 = null          // 사진을 버리고 재시도 — 사용자에게 알림 없음
  try { this._lsSet(STORE_KEY, list) }
  catch (e2) { list.shift(); throw new Error('저장 공간이 부족해요...') }
}
```

`measure/page.tsx:522`에서 저장 썸네일은 640px / JPEG q0.6 → base64 약 **60~120KB/건**입니다.
localStorage 기본 할당량이 약 5MB이므로 **대략 40~80건이면 한도에 도달**합니다.

**영향**: 한도 도달 이후 저장되는 기록은 **사진 없이 조용히 저장**됩니다.
사용자는 성공 토스트(`SAVE_SUCCESS`)를 보지만 나중에 일지를 열면 사진이 없습니다. 실패 안내가 전혀 없습니다.

또한 첫 번째 재시도도 실패하는 경우가 흔합니다 — 용량을 넘긴 주범은 새 항목 하나가 아니라 **누적된 리스트 전체**이기 때문에, 새 항목의 사진만 지워도 여전히 한도를 넘습니다.

---

## 3. 심각도 **중간** (12건)

### M-1. 카카오 REST API 키가 브라우저에 노출
**파일**: `src/services/LocationService.js:27`
```js
const key = process.env.NEXT_PUBLIC_KAKAO_MAP_KEY
fetch(`https://dapi.kakao.com/v2/local/geo/coord2address.json?...`,
      { headers: { Authorization: `KakaoAK ${key}` } })
```
`NEXT_PUBLIC_` 접두사라 **클라이언트 번들에 그대로 실려 나갑니다.** `KakaoAK` 스킴은 REST API 키용이며 도메인 제한이 걸리지 않습니다. 제3자가 키를 추출해 쿼터를 소진시킬 수 있습니다.
이미 `/api/geocode`에 Nominatim 서버 프록시 패턴이 있으므로, 역지오코딩도 같은 방식으로 옮기면 됩니다.

### M-2. MapScreen이 기존 지오코딩 프록시를 우회
**파일**: `src/components/map/MapScreen.tsx:170-180`
브라우저에서 `nominatim.openstreetmap.org`를 직접 호출합니다. `/api/geocode` 프록시가 이미 존재하고 거기엔 정책상 필수인 `User-Agent` 헤더까지 붙어 있는데, MapScreen만 이를 쓰지 않습니다. 브라우저에서는 `User-Agent`를 지정할 수 없어 **Nominatim 이용 정책 위반이며 IP 차단 대상**입니다.

### M-3. 레이트 리밋이 인메모리 전용 — 다중 인스턴스에서 무력화
**파일**: `src/lib/rateLimit.ts`, `src/app/api/auth/login/route.ts:13`
`Map` 기반이라 PM2 cluster·다중 컨테이너·서버리스에서는 **프로세스 수만큼 한도가 곱해집니다.** 코드 주석도 이를 인지하고 있습니다. 영향 범위: 로그인 브루트포스(5분 10회), OTP 발송 비용(10분 5회), AI 스캔 비용(분당 45회), 업로드(일 50회). 재시작 시 초기화되는 것도 일일 한도 항목에서는 문제입니다.

### M-4. 계측 기록 ID 충돌 가능
**파일**: `src/services/DatabaseService.js:39` — `id: Date.now()`
같은 밀리초에 2건 저장되면 ID가 겹칩니다. `deleteMeasurement`는 `filter((m) => m.id !== id)`라서 **한 건을 지우면 중복 ID 기록이 함께 삭제**됩니다.

### M-5. DiarySheet 열 때마다 데이터 2회 로드
**파일**: `src/components/DiarySheet.tsx:118-137`
`open` 이펙트가 `setSpecies("")` + `loadPage(1,"",true)`를 호출하고, `species` 이펙트가 다시 `loadPage(1, species, true)`를 호출합니다. 시트를 열 때마다 동일 조회가 2번 실행됩니다.

### M-6. 무한 스크롤 중복 페치 가능
**파일**: `src/components/DiarySheet.tsx:140-155`
IntersectionObserver 콜백이 로딩 중인지 확인하지 않습니다. 리렌더 전에 교차가 2회 발생하면 같은 페이지가 두 번 추가되어 **목록에 중복 항목**이 생깁니다. 가드용 `loading`/`fetching` ref가 없습니다.

### M-7. 이미지 최적화 미적용 (`<img>` 111곳 vs `next/image` 11곳)
**파일**: `.eslintrc.json` — `"@next/next/no-img-element": "off"`
모바일 우선 앱인데 대부분의 이미지가 `srcset`·lazy loading·자동 리사이즈 없이 원본으로 나갑니다. `next.config.mjs`의 AVIF/WebP 설정도 `next/image`를 거치지 않으면 적용되지 않습니다.

### M-8. base64 이미지를 DB 컬럼에 저장
**파일**: `prisma/schema.prisma:28`, `src/app/api/fishing-spots/route.ts:88`
```prisma
avatarUrl String? @db.Text   // "base64 data URI로 저장될 수 있어" (주석)
```
```ts
photoUrl: str(b.photoUrl, 100000),   // 100,000자 허용 = base64 이미지 통째로
```
행 크기가 커져 목록 쿼리(`take: 200`)의 응답이 수 MB로 부풀고, JSON 직렬화·전송 비용이 커집니다. `/api/upload`로 파일화한 뒤 URL만 저장하는 편이 일관됩니다 (이미 그 경로가 존재).

### M-9. 조용한 예외 처리 37곳
**파일**: 전역 (`catch {}` 36곳 + 빈 catch 1곳)
에러가 로그도 사용자 피드백도 없이 사라집니다. H-4의 업로드 실패가 대표 사례입니다. 최소한 `console.warn`이나 `/api/debug/client-error` 리포팅은 필요합니다.

### M-10. LiveScanCamera 이펙트 의존성 누락 (stale closure)
**파일**: `src/components/LiveScanCamera.tsx:1081`
스캔 폴링 이펙트가 `refType`·`testBall`을 의존성에서 누락했습니다. 측정 중 기준물(볼↔키링)을 전환해도 **폴링은 옛 값으로 계속 요청**하므로, 서버가 잘못된 프롬프트(구체 vs 평면 디스크)로 판정할 수 있습니다. 키링은 타원 왜곡 검증(`KEYRING_MIN_ASPECT`)이 필요해 잘못된 계측값으로 이어집니다.

### M-11. 접근성 — 아이콘 전용 버튼에 접근 가능한 이름 부족
전체 `<button>` 596개 중 `aria-label`은 158개입니다. 카메라 닫기(X), 재시도, 플래시 토글, 지도 컨트롤 등 **텍스트 없는 아이콘 버튼 상당수가 스크린리더에서 "버튼"으로만 읽힙니다.**

### M-12. 포인트 충전 페이지와 API의 권한 불일치
**파일**: `src/app/api/points/charge/route.ts:12`, `src/app/me/points/charge/page.tsx:16`
API는 `SUPER_ADMIN`만 허용하는데, 페이지는 `points_enabled`가 켜져 있으면 일반 회원에게도 열립니다. 포인트 제도를 켜는 순간 **일반 회원은 충전 화면에 들어가서 403 "포인트 충전 서비스는 준비 중입니다"를 만나게** 됩니다. (PG 미연동 상태의 의도적 가드 자체는 타당합니다.)

---

## 4. 심각도 **낮음** (7건)

### L-1. 루트 디렉터리 정리 필요
`.bat`/`.ps1`/`.vbs`/`.py` 스크래치 스크립트 약 30개, 로그 파일(`cloudflared.err.log`, `dev-server.out.log`, `git_result.txt`, `push_result.txt`), `test_delete_me.tmp`가 저장소 루트에 방치되어 있습니다. 일부 로그는 로컬 경로·호스트 정보를 담을 수 있습니다.

### L-2. `console.log` 20곳이 프로덕션 코드에 잔존

### L-3. 핵심 데이터 계층이 `.js` — 타입 검사 사각지대
`src/services/*.js` 7개 파일(DatabaseService, SyncService, MeasurementApiService, LocationService, AutoTagService, TideService, WeatherService)이 순수 JS입니다. `tsconfig.json`의 `strict: true`가 여기에는 미치지 않습니다. **H-1·H-2·H-6이 전부 이 파일들에서 나왔다는 점이 시사적입니다.**

### L-4. `any` 사용 259곳 / `useState<any>` 10곳
`ball`, `result`, `points`, `myPoints`, `stats` 등 핵심 상태가 `any`입니다. 타입 체크가 통과해도 이 경로는 검증되지 않습니다.

### L-5. 대형 파일 — 유지보수/테스트 난도
`LiveScanCamera.tsx` 2,275줄 / `measure/page.tsx` 1,695줄 / `MapScreen.tsx` 1,424줄 / `BallLinkSection.tsx` 1,409줄 / `groups/[id]/page.tsx` 1,382줄.

### L-6. 미들웨어가 로그인 사용자도 항상 `/landing`으로 리다이렉트
**파일**: `src/middleware.ts` — matcher가 `/` 하나뿐이라 비용은 작지만, 로그인 상태에서 루트 진입 시 항상 랜딩을 거칩니다.

### L-7. `measure/page.tsx` 저장 시 상태 설정 순서가 암묵적 타이밍에 의존
**파일**: `src/app/measure/page.tsx:583-600`
`fetch(...).then(d => setSpotCatchId(d.catchId))` 직후 동기적으로 `setSpotCatchId(null)`을 호출합니다. 현재는 마이크로태스크 순서 덕에 의도대로 동작하지만, 의존 관계가 코드에 드러나지 않아 리팩터링 시 깨지기 쉽습니다.

---

## 5. 기능별 점검 결과

### 5.1 AI 측정 (`LiveScanCamera`, `/measure`)
**양호한 점**
- `getUserMedia` 3단계 폴백(environment ideal → environment → any), 10초 워치독, DOMException 종류별 한국어 안내
- iOS Safari 대응이 꼼꼼함 — `muted`/`playsinline` DOM 직접 설정, autoplay 폴링 폴백(500ms), 1.5초 하드 폴백
- 스트림·폴링·AbortController 정리가 언마운트/재시도 경로 모두에 존재 (메모리 누수 없음)
- EXIF 회전을 `createImageBitmap({imageOrientation:'from-image'})`로 처리하고 구형 브라우저 폴백 보유
- `refineYellowBallRadius`의 16방향 방사 스캔 + 중앙값 + 55~110% 범위 클램프 — 오측 방어가 견고
- `REF_DIAMETER_MM` 상수화로 "지름 40mm ≠ 반지름" 불변식이 명시됨
- 빈 프레임 스킵(`IDLE_SKIP_LIMIT`)으로 AI 호출 비용 절감, 안전망도 있음
- `/api/measure/scan`에 `requireUser` + 분당 45회 레이트 리밋

**문제**: H-4, H-5, H-6, M-10, L-5

### 5.2 스마트피싱 맵 (`MapScreen`, `RecordingProvider`)
**양호한 점**
- `RecordingProvider`의 정리 로직이 철저함 — `clearWatch`, `clearInterval`, wake lock 세대 카운터로 release/request 경합 방지
- localStorage 영속화(`ipnak_rec_v1`)로 새로고침·앱 재시작에도 세션 유지
- 30초 보완 폴링으로 `watchPosition` 스로틀 시 직선 구간 완화, `accuracy > 30` 노이즈 필터, `medianOfThree` 스무딩
- 지오코딩 검색에 AbortController + 500ms 디바운스로 race 방지
- `status === "idle"` 시 배경모드/배너 자동 해제

**문제**: M-2, L-5

### 5.3 계측일지 (`DiarySheet`, `/diary`)
**문제**: H-1, H-2, H-6, M-4, M-5, M-6 — **점검 항목 중 가장 취약한 영역입니다.**

### 5.4 커뮤니티 / 마켓 / 마이
**양호한 점**
- 소유권 검증이 일관됨 — `market/[id]` PATCH·DELETE, `posts/[id]` 모두 `sellerId/authorId !== user.id → 403`
- `posts/[id]` 삭제 시 타인 댓글 존재 여부까지 확인
- `FeedCard`가 `memo()`로 감싸져 있고 `useMemo`/`useCallback` 적용 (`slides`, `walkingData`, `share`)
- 포인트 시스템이 견고 — `prisma.$transaction` + `{ increment }` 원자적 증감 + 음수 시 throw 롤백, `lockUserRow`로 일일 한도 race 방지, 닉네임 중복 시 선물 차단(`DUPLICATE_NICKNAME`)

**문제**: M-7, M-12

### 5.5 어장포인트 저장/조회
**양호한 점** — 이번 점검에서 가장 깔끔한 라우트입니다.
- GET/POST/PATCH/DELETE 전부 `requireUser` + `userId` 스코프
- 좌표 검증(`|lat|<=90`, `|lng|<=180`), 문자열 길이 제한, `sourceType` 화이트리스트
- PATCH가 `"key" in b` 방식의 진짜 부분 수정
- 테이블 부재 환경에서도 GET이 빈 배열로 graceful degrade

**문제**: M-8 (`photoUrl` 100,000자)

### 5.6 인증 (로그인/회원가입)
**양호한 점**
- bcrypt(cost 10), httpOnly + sameSite=lax + 운영 시 secure 쿠키, 30일 만료
- 로그인 실패 메시지가 이메일/비밀번호를 구분하지 않음 (사용자 열거 방지)
- `x-real-ip` 우선 → XFF **마지막** IP 사용 (스푸핑 방어가 정확함)
- 회원가입 P2002 경쟁 조건을 409로 정돈, 비밀번호 정규식(영문+숫자+특수문자 8자 이상)
- OTP에 이중 레이트 리밋(번호별 + IP별), SMS 실패 시 레코드 롤백
- `getCurrentUser`가 DB 예외를 삼켜 RootLayout SSR 크래시 방지 (주석에 근거 명시)
- 만료 세션 자동 정리 + `Session.expiresAt` 인덱스 존재

**문제**: H-3, M-3

---

## 6. UI/UX · 성능

### 모바일 최적화 — 대체로 양호
- `viewport`에 `viewportFit: "cover"`, `themeColor` 설정
- `maximumScale: 1` 제거 (iOS 확대/당겨서 새로고침 차단 회피) — 접근성 관점에서 올바른 선택
- `--safe-bottom` / `--safe-top` CSS 변수로 노치 대응, 하단 nav도 `calc(5rem + env(safe-area-inset-bottom))`
- LiveScanCamera가 가로/세로 모두 풀스크린 지원, `matchMedia("(orientation: landscape)")`로 자동 동기화
- 오버레이 정렬을 위해 video/canvas 모두 `object-cover`로 통일 (좌표 일치)

### 성능
- **리렌더링**: `FeedCard`만 `memo()` 적용. `MePageTabs`(943줄), `MapScreen`(1,424줄) 등 대형 컴포넌트는 미적용이나, 리스트 항목 컴포넌트가 아니라 영향은 제한적입니다.
- **메모리 누수**: 점검 범위에서 **발견되지 않았습니다.** 타이머·워처·스트림·이벤트 리스너·IntersectionObserver 모두 cleanup이 있습니다.
- **이미지**: M-7, M-8이 가장 큰 개선 여지입니다.
- **번들**: `onnxruntime-web`을 정적 import하지 않고 모델 존재 확인 후 동적 로드 — 모델 미배포 시 다운로드조차 하지 않는 설계가 좋습니다.

### 접근성
- M-11(아이콘 버튼 접근 이름)이 주요 개선점입니다.
- 색상 대비, 포커스 링, 키보드 내비게이션은 이번 정적 점검 범위 밖입니다 — 실기기/스크린리더 검증을 별도 권장합니다.

---

## 7. 권장 조치 순서

### 1순위 — 데이터 무결성 (즉시)
1. **H-5**: `catch/route.ts:33` 서울 좌표 기본값 제거 — 한 줄 수정으로 지도 오염 차단. 가장 저비용/고효과
2. **H-4**: picsum 폴백 제거 + 업로드 실패를 사용자에게 노출
3. **H-2**: `STORE_KEY`에 userId 포함 (`ipnak_measurements:${userId}`) 또는 로그아웃 시 삭제
4. **H-3**: 가입 시 인증 레코드 만료 확인 + 소모(삭제/플래그) 처리

### 2순위 — 아키텍처
5. **H-1**: 계측일지 서버 동기화 활성화. 신규 API를 만들기보다 **이미 동작 중인 `/api/catch` 데이터를 일지 화면의 소스로 병합**하는 편이 경로가 짧습니다
6. **H-6**: 사진을 `/api/upload`로 올리고 localStorage에는 URL만 저장 (H-1과 함께 해결 가능)
7. **M-1**: 카카오 역지오코딩을 서버 프록시로 이전 + 키 재발급

### 3순위 — 품질
8. M-3(분산 레이트 리밋), M-10(이펙트 의존성), M-5/M-6(중복 로드), M-11(접근성)
9. L-3(services를 TS로 이전) — H-1·H-2·H-6의 재발 방지 효과

---

## 8. 검증 명령 재현

```bash
npx tsc --noEmit        # exit 0, 출력 없음
npm run build           # exit 0
npx next lint           # exit 0, 경고 9건
```

---

*본 보고서는 정적 분석과 코드 리뷰 기반이며, 코드는 수정하지 않았습니다.*
*런타임 동작(실기기 카메라, GPS 정확도, 스크린리더)은 별도 실측 검증을 권장합니다.*
