# 입낚 프로젝트 점검 보고서

> 점검일: 2026-08-06  
> 점검 모델: Claude Fable  
> 점검 범위: API 라우트 20여 개, 핵심 라이브러리(auth/points/rateLimit/prisma/queries), Prisma 스키마, 주요 페이지·컴포넌트(피드/마켓/포인트/채팅), 디자인 토큰(tailwind.config.ts)

---

## 📋 요약

| 등급 | 건수 | 설명 |
|------|------|------|
| 🔴 Critical | 3건 | 즉시 수정 필요 — 보안 취약점 또는 데이터 손실 위험 |
| 🟠 Major | 9건 | 빠른 수정 권장 — 기능 버그 또는 성능 문제 |
| 🟡 Minor | 12건 | 개선 권장 — 코드 품질·일관성·소규모 버그 |

---

## 🔴 Critical (즉시 수정 필요)

| # | 파일 | 현재 상태 | 문제점 | 개선 방안 |
|---|------|----------|--------|----------|
| C1 | `src/app/api/upload/route.ts` | 확장자만 검사하고(`ALLOWED` 배열, L13·L32-33), 허용 외 확장자는 **강제로 `.jpg`로 rename**하여 `public/uploads`에 저장. 파일 내용(매직 바이트)·MIME 검증 없음, 회원당 업로드 횟수/총량 제한 없음 | 임의 바이너리(실행파일·HTML·스크립트)를 `.jpg`로 위장해 서버에 무제한 호스팅 가능. 8MB × 무제한 반복 업로드로 디스크 고갈(DoS) 가능. `.heic`→`.jpg` rename은 변환이 아니라서 깨진 이미지가 저장됨(L33) | ① sharp 등으로 실제 이미지 디코드-재인코딩(변환 겸 검증) ② 회원별 rate limit(`lib/rateLimit.ts` 재사용) ③ HEIC는 실제 변환 또는 거부 |
| C2 | `src/app/api/auth/naver/callback/route.ts` (L132-146), `src/app/api/auth/kakao/callback/route.ts` (L126-140) | 소셜 프로필의 email이 기존 회원 email과 일치하면 **검증 없이 자동으로 naverKey/kakaoKey를 연동하고 즉시 세션 발급** | 카카오는 `is_email_verified`/`email_needs_agreement`를 확인하지 않음. 공격자가 피해자 이메일로 등록된 소셜 계정을 확보하면 기존 입낚 계정을 그대로 탈취할 수 있는 계정 연결(account-linking) 취약 패턴 | 이메일 검증 플래그 확인(카카오 `is_email_verified`) + 자동 연동 대신 "기존 계정과 연결하시겠습니까?" 재인증(비밀번호 확인) 단계 삽입 |
| C3 | `src/app/api/auth/signup/route.ts` + `src/lib/points.ts` | 회원가입에 rate limit·이메일 인증·captcha가 전혀 없음. 가입 즉시 세션 발급. `awardCommentReward`(댓글 +10P×20회/일), `giftPoints`(제한 없음) | **포인트 파밍 체인 성립**: 스크립트로 계정 대량 생성 → 각 계정으로 댓글 적립 → `POST /api/points/gift`로 본계정에 몰아주기. 1원=1P(`WON_PER_POINT`)라 실질 금전 가치가 있는 포인트가 무제한 채굴됨. 부수적으로 bcrypt(cost 10) 무제한 호출로 CPU DoS도 가능 | ① 가입 IP rate limit + 이메일 인증 ② 선물 기능에 일일 한도·가입 후 유예기간 ③ 적립 포인트와 충전 포인트 분리(선물·결제는 충전분만 허용) |

---

## 🟠 Major (빠른 수정 권장)

| # | 파일 | 현재 상태 | 문제점 | 개선 방안 |
|---|------|----------|--------|----------|
| M1 | `src/app/api/market/route.ts` (GET, L39-45), `src/app/market/page.tsx` (L26-35) | `marketListing.findMany`에 `take`/cursor 없음. 목록 페이지는 전 매물의 `description` 전문까지 로드(클라이언트 검색용) | 매물이 수천 건이 되면 목록 조회가 전체 테이블 + 본문 텍스트를 매번 직렬화 — 응답 폭증·서버 부하. 피드는 cursor 페이지네이션이 잘 돼 있어(`getFeedPostsPage`) 마켓만 뒤처짐 | 마켓도 cursor 페이지네이션 도입, 본문 검색은 서버 API(`?q=`)로 일원화하고 목록에서 `description` 제외 |
| M2 | `src/app/api/market/chats/[id]/messages/route.ts` (POST L58) + `src/components/market/MarketChatRoom.tsx` (L191, L205-210) | 메시지 본문이 `"[시스템]"`으로 시작하면 알림 생략, 클라이언트는 시스템 말풍선으로 렌더링. 서버는 누가 보냈든 그대로 저장 | **구매자가 직접 "[시스템] 거래가 완료됐습니다"를 전송해 위조 시스템 메시지 표시 가능**(사기 악용 소지). 시스템 메시지가 일반 사용자 입력과 같은 채널로 처리됨 | 시스템 메시지는 별도 `type` 컬럼/서버 전용 경로로 생성하고, 사용자 입력의 `[시스템]` prefix는 저장 시 제거 |
| M3 | `src/app/api/posts/route.ts` (L82-96) | `post.create` 실패 시 `kind/title/body/boardCategory/location/fishSpecies`를 **제거한 legacy 데이터로 조용히 재시도** | 일시 오류로 폴백이 타면 일상피드(GENERAL)·조행기 데이터가 kind 기본값 `FEED`로 잘못 저장되는 데이터 훼손 경로. 또 503 메시지에 "`npm run db:push` 실행 후…"라는 내부 운영 지시가 최종 사용자에게 노출됨(L85) | DB 마이그레이션이 완료된 운영 환경이라면 폴백 자체를 제거하고, 사용자 메시지는 일반 오류 문구로 교체 |
| M4 | `src/app/api/posts/[id]/like/route.ts` (L7-34) | `findUnique`→`create` 비원자 토글, 전체를 감싼 catch가 모든 오류를 404로 응답 | 더블탭/더블클릭 시 unique(`postId_userId`) 위반(P2002)이 발생하고 사용자는 멀쩡한 글에서 "게시글을 찾을 수 없습니다" 404를 받음. 또 좋아요를 껐다 켤 때마다 알림이 새로 생성돼 알림 스팸 | upsert/delete 패턴 또는 P2002를 idempotent 성공으로 처리, 좋아요 알림은 (postId, actorId) 기준 중복 방지 |
| M5 | `src/lib/points.ts` (`awardPostReward` L153-163, `awardCommentReward` L220-230) | `count` 조회 후 별도 트랜잭션으로 적립 — 카운트와 적립이 비원자 | 병렬 요청(연속 글 등록 봇)으로 하루 5회/20회 한도를 초과 적립 가능. C3 파밍과 결합 시 피해 확대 | 카운트+적립을 하나의 `$transaction`으로 묶거나 일일 적립 집계 테이블에 unique 제약 |
| M6 | `src/app/api/me/route.ts` (L24) | `avatarUrl`을 길이 제한 없이 저장(다른 필드는 `slice()`로 제한). 스키마 주석상 base64 data URI 저장 전제(LONGTEXT) | 수 MB base64 아바타가 DB에 저장되고, `feedInclude`의 `author.avatarUrl`을 통해 **모든 피드/댓글 응답에 그대로 실려 나감** — 페이로드·DB 폭증 | avatarUrl은 `/api/upload` 경유 URL만 허용(정규식 검증 + 길이 제한), base64 저장 금지 |
| M7 | `src/app/api/auth/login/route.ts` (L13-18) 등 | rate limit 키가 `x-forwarded-for` 첫 값 — 클라이언트가 임의 조작 가능한 헤더 | 프록시가 헤더를 덮어쓰지 않는 배포에서는 헤더 값을 바꿔가며 로그인 브루트포스 rate limit 우회 가능 | 신뢰 가능한 프록시 뒤에서만 XFF 사용하도록 고정(Nginx `set_real_ip`), 로그인은 IP+이메일 복합 키로 제한 |
| M8 | `src/app/api/auth/naver/callback/route.ts` (L33-36), kakao 동일 | `baseUrl`이 `"https://ipnak.com"` / `"http://localhost:3010"` 하드코딩. `process.env.NAVER_CLIENT_ID!` non-null 단언 | 스테이징/도메인 변경 시 OAuth 전체가 조용히 깨짐. env 미설정 시 `client_id=undefined` 문자열이 전송돼 원인 추적 어려움 | `APP_BASE_URL` env로 추출, 기동 시 필수 env 존재 검증 |
| M9 | `src/app/market/page.tsx` (L56-74) | 답장 필요 배지 계산을 위해 사용자의 **모든 채팅방 + 각 마지막 메시지**를 로드 | 채팅이 누적될수록 마켓 목록 진입마다 무거운 쿼리 2개 실행. `MarketMessage.readAt`이 이미 있으므로 미읽음 count 한 방으로 대체 가능 | `marketMessage.count({ where: { readAt: null, senderId: { not: me }, chat: {...} } })` 집계 쿼리로 교체 |

---

## 🟡 Minor (개선 권장)

| # | 파일 | 현재 상태 | 문제점 | 개선 방안 |
|---|------|----------|--------|----------|
| m1 | `src/middleware.ts` | `matcher: ["/"]`에서 무조건 `/landing` 리다이렉트 | 로그인 회원도 루트 진입 시 매번 랜딩으로 이동 — 재방문 UX 저하 | 세션 쿠키 존재 시 `/home`으로 분기 |
| m2 | `src/app/feed/page.tsx` (L20) | `bassOnlyMode` 필터를 **페이지네이션 이후** 클라이언트 측 배열 filter로 적용 | 첫 페이지가 20건 미만으로 보이거나, `nextCursor`가 필터 전 기준이라 스크롤 시 목록이 튈 수 있음 | 배스 필터를 `getFeedPostsPage`의 where 조건으로 내림 |
| m3 | `prisma/schema.prisma` (Comment L262) | `parent Comment? @relation("Replies", ...)`에 onDelete 미지정(옵셔널 → SetNull) | 부모 댓글 삭제 시 대댓글이 일반 댓글로 승격되어 문맥을 잃음 | `onDelete: Cascade` 또는 소프트 삭제("삭제된 댓글입니다") |
| m4 | `src/app/api/posts/[id]/comments/route.ts` (L88-89) | 적립 조건이 `post?.authorId !== user.id` — post 조회 실패(null) 시에도 true | 게시글 작성자 조회 실패 시에도 적립 실행. 주석 "(+10P, 한도 없음)"도 실제(20회/일)와 불일치 | `post && post.authorId !== user.id`로 수정, 주석 정정 |
| m5 | `src/app/api/me/route.ts` (L17-19, L26-28) | 닉네임 2자 미만이면 조용히 무시 → 닉네임만 보낸 경우 "변경할 내용이 없습니다" 400 | 사용자는 왜 실패했는지 알 수 없음 | 유효성 실패는 명시적 오류("닉네임은 2자 이상")로 응답 |
| m6 | `src/app/api/market/[id]/route.ts` (L23) | PATCH의 `category`가 화이트리스트 검증 없이 임의 문자열 허용(POST는 기본값 처리만) | 존재하지 않는 카테고리로 저장되면 필터 탭에서 유실 | `MARKET_CATEGORIES` 키 목록으로 검증 |
| m7 | `src/components/market/MarketChatRoom.tsx` (L47-51) | 4초 setInterval 폴링, 탭 비활성화 시에도 지속. GET마다 서버가 `updateMany`(읽음 처리) 쓰기 실행 | 불필요한 서버 부하·배터리 소모 | `document.visibilityState` 체크, 미읽음 있을 때만 updateMany |
| m8 | `src/components/FeedCard.tsx` (L328, 433, 461…) | 피드 본문 이미지가 전부 순수 `<img>` (next/image는 6개 파일에서만 사용) | 리사이즈·webp 변환·lazy 최적화 미적용(업로드 시 1200px 압축은 있음). 컴포넌트 간 이미지 처리 방식 이원화 | 공용 `<AppImage>` 래퍼로 통일(커스텀 로더 포함) |
| m9 | `src/app/api/points/route.ts` | 경로는 `/api/points`인데 실제 응답은 **낚시 포인트(지도)** 데이터, 잔액은 `/api/points/balance` | "포인트(재화)" API 트리 아래 이질적 리소스 — 유지보수 혼동 | `/api/fishing-points`로 분리 |
| m10 | `src/lib/prisma.ts` (L7) | `log: dev ? ["error"] : ["error"]` — 삼항 양쪽이 동일 | 의도(개발 시 query 로그?)가 코드에 드러나지 않음 | dev는 `["query","warn","error"]`로 구분하거나 삼항 제거 |
| m11 | `prisma/schema.prisma` | `ShareEvent`·`Report`·`AdminLog`에 인덱스 없음, `Notification`은 `[userId, read]`만 있고 정렬 컬럼(createdAt) 미포함 | 데이터 누적 시 목록 조회 filesort. 알림은 30건 take라 당장은 경미 | `@@index([userId, createdAt])` 등 조회 패턴 기준 보강 |
| m12 | `src/app/api/auth/signup/route.ts` (L36) | 닉네임 unique 미보장(스키마도 non-unique), 자동 생성 닉네임 충돌 가능 | 닉네임 중복 시 `giftPoints`가 `DUPLICATE_NICKNAME`으로 실패하는 등 하류 기능이 흔들림 | 닉네임 unique 제약 또는 선물 대상 지정을 이메일/ID 기반으로 유도 |

---

## 💡 UX 개선 제안

1. **채팅 진입 흐름** (`MarketDetailActions.tsx` L31-39): `existingChatId`가 있으면 `router.push` 후 `busy`가 영원히 true로 남음(해제 코드 없음) — 뒤로가기로 돌아오면 버튼이 잠긴 채로 보일 수 있음. push 후에도 finally에서 busy 해제 권장.

2. **포인트 충전 UX** (`api/points/charge/route.ts`): 일반 회원에게 403 "준비 중입니다"를 API에서 돌려주는데, `me/points` 화면에서 충전 버튼 자체를 숨기거나 "준비 중" 배지로 선제 안내하면 헛클릭이 줄어듦.

3. **좋아요/댓글 알림 문구**: 알림 body에 게시글 제목/캡션 앞부분을 포함하면(현재 "OO님이 좋아요를 눌렀습니다"만) 어떤 글인지 바로 식별 가능.

4. **댓글 대댓글 삭제**: m3과 연계 — "삭제된 댓글입니다" 플레이스홀더 처리로 스레드 문맥 유지.

5. **미들웨어 리다이렉트**: m1과 연계 — 로그인 사용자가 `/`으로 접근 시 `/home`으로, 비로그인은 `/landing`으로 분기하는 UX 개선.

---

## 🎨 디자인 일관성 점검

- **브랜드색 토큰화는 잘 됨**: `tailwind.config.ts`에서 `orange/amber/yellow` 3계열이 모두 `500: #eab308`로 리매핑되어 `bg-orange-500` 클래스가 곧 브랜드색. 다만 이름이 `orange`라 신규 개발자가 오해할 여지가 큼 — `brand` alias 추가 권장.

- **노랑 위 글자색 불일치(접근성)**: `MarketChatRoom.tsx` L218 말풍선과 `ui.tsx` Chip(L58)은 `bg-orange-500 text-white`(#eab308 위 흰 글자, 대비 약 1.9:1로 WCAG 미달), 반면 `MarketDetailActions.tsx` L83은 인라인 `background:"#eab308", color:"#000"`. **노랑 배경에는 검정/네이비 텍스트로 통일** 필요.

- **인라인 hex 하드코딩**: `MarketDetailActions.tsx`(#eab308, #374151), 마켓 계열 컴포넌트의 `bg-[#0d1b2a]`, `bg-[#1e3148]`, `bg-[#162538]`, `bg-[#0a1220]` 등 구 네이비 팔레트가 하드코딩되어 있는데, 테마의 다크 서피스는 `surface`(#161616 계열) — 다크 톤이 두 계통으로 혼재. 토큰 클래스로 이관 권장.

- **아이콘**: Lucide 라인 아이콘으로 매우 일관됨(`strokeWidth={1.5}` 통일도 양호). 단 채팅 시스템 메시지의 이모지("🎉", "🙋")는 라인 아이콘 톤과 어긋남 — M2 수정 시 함께 정리 권장.

---

## ✅ 잘 된 점

- **인증/세션**: httpOnly + sameSite=lax + production secure 쿠키, 세션 토큰 `randomBytes(32)`, 만료 세션 백그라운드 청소(`purgeExpiredSessions`), `getCurrentUser`에서 `passwordHash` 제거 후 반환.
- **OAuth CSRF**: 네이버·카카오 콜백 모두 state 쿠키 검증, 신규 가입 시 user+session을 트랜잭션으로 묶어 고아 계정 방지.
- **포인트 원장 설계**: `applyPoints`가 `increment` 원자 증감 + `balanceAfter` 기록 + 음수 잔액 시 트랜잭션 롤백. 충전은 PG 연동 전 SUPER_ADMIN 전용으로 잠가둔 판단이 훌륭함. 워킹 피드 잠금 해제도 트랜잭션 내 중복 확인.
- **가시성 검증의 서버 강제**: `lib/queries.ts`의 `getPost`/`getLogPost`가 PRIVATE/FOLLOWERS/hidden을 URL 직접 접근까지 차단, 댓글 API(`canViewPost`)도 동일 규칙 적용. 잠긴 워킹 피드는 서버에서 본문·좌표·이미지를 차단(클라이언트 우회 불가).
- **쿼리 최적화 흔적**: `feedInclude`에서 열람자 것만 likes/bookmarks 조회 + `_count` 집계, cursor 페이지네이션, product 큰 컬럼 제외 등 N+1/페이로드 최적화가 의식적으로 되어 있음.
- **AI 어종인식**: 로그인 필수 + 회원 단위 rate limit + 이미지 크기 상한 + 타임아웃 + 실패 사유 분류까지 방어적 설계가 모범적.
- **XSS/SQL 인젝션 표면 없음**: `dangerouslySetInnerHTML` 사용 0건, Prisma ORM으로 SQL 인젝션 표면 없음.
