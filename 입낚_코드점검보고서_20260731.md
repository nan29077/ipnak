# 입낚 프로젝트 코드 점검 보고서

> 점검일: 2026-07-31 | 모델: claude-fable-5 | 점검 범위: Next.js 14 App Router + Prisma + SQLite 전체  
> **코드 수정 없이 점검 및 보고만 수행**

---

## 요약

| 우선순위 | Critical | Major | Minor |
|---|---|---|---|
| 1. AI 카메라 계측 | 4 | 7 | 5 |
| 2. 상품 판매/구매 | 3 | 6 | 4 |
| 3. 계정별 피드 권한 | 2 | 4 | 3 |
| 4. 전반적 코드 품질 | 2 | 5 | 5 |
| **합계** | **11** | **22** | **17** |

### 즉시 조치 필요 사항 (Critical 요약)

1. **AI 스캔이 현재 사실상 동작 불가** — OpenAI 키 미설정 시 실패 사유가 사용자에게 전달되지 않음. 개발 로그 응답속도(55~66ms)로 보아 실제 추론이 한 번도 수행되지 않고 있음.
2. **계측 기록이 localStorage에만 저장** — 계정 간 데이터 혼입, 기기 변경 시 전량 유실됨.
3. **워킹 피드 유료 잠금이 클라이언트 오버레이에 불과** — API 응답에 GPS 좌표·사진 URL이 그대로 포함되어 페이월 우회 가능.
4. **결제가 `Math.random() < 0.9` 목(mock)인데 주문을 `PAID`로 저장** — 실제 청구 없이 재고 차감.
5. **입낚볼 주문 경로가 2개로 분기** — `/shop/ipnak-ball` 구매는 관리자가 볼 수 없고 배송 처리 불가.
6. **시크릿 키가 작업트리에 평문 저장** — SESSION_SECRET 유출 시 저장된 모든 자격증명 복호화 가능.

---

## 1. AI 카메라 계측 기능 (1순위)

**구성 흐름**: `/measure` 페이지 → `LiveScanCamera`(2초 폴링) 또는 갤러리 업로드 → `POST /api/measure/scan`(OpenAI Vision) → `MeasurementCalculator`/`weightEstimation` 계산 → `DatabaseService`(localStorage) + `POST /api/catch`(서버 DB)

### 🔴 Critical

**[1-C-01] AI 키 미설정 시 "정상 응답(200)"으로 실패를 감춤 — 계측 기능 전체가 현재 무력화**

- **위치**: `src/app/api/measure/scan/route.ts:90-94`, `src/app/measure/page.tsx:216-224`, `src/components/LiveScanCamera.tsx:506-512`
- **문제**: 키가 없으면 `{ ok:false, reason:"no-ai-key" }`를 HTTP 200으로 반환. 클라이언트는 `!data.ok` → 무조건 `throw new Error("scan-unreliable")` → "자동 측정이 어려운 사진이에요" 안내.
- **영향**: 개발 서버 로그에서 `POST /api/measure/scan 200 in 55~66ms`가 반복됨. OpenAI 왕복이 있었다면 수백 ms~수 초가 걸려야 하므로 **현재 AI 스캔은 한 번도 실제 추론을 하지 않고 즉시 실패 응답만 돌려주고 있음**. `/api/ai/status`(`openaiConfigured`)가 이미 존재하는데 측정 진입 전 확인하지 않음.

---

**[1-C-02] 계측일지가 userId 구분 없이 localStorage에 저장 — 계정 간 데이터 혼입 및 유실**

- **위치**: `src/services/DatabaseService.js:8,36`, `src/app/measure/page.tsx:382-396`
- **문제**: 측정 결과를 `localStorage['ipnak_measurements']`에 userId 없이 저장. 동일 브라우저에서 계정 전환 시 다른 사용자의 계측일지(사진·GPS 좌표 포함)가 노출됨.
- **영향**: 로그아웃·캐시 삭제·기기 변경 시 전량 유실. 다른 기기에서 조회 불가. 타인의 낚시 포인트 정보가 노출될 수 있음.

---

**[1-C-03] 동기화 경로가 영구 비활성 — "나중에 동기화됩니다" 안내가 거짓**

- **위치**: `src/services/MeasurementApiService.js:7`, `src/services/SyncService.js:17`
- **문제**: `USE_MOCK = process.env.NEXT_PUBLIC_USE_MOCK_API !== 'false'`이며 `.env`에 해당 변수가 없어 항상 mock. `SyncService.syncPendingMeasurements()`는 `if (measurementApi.isMock) return`으로 즉시 종료됨.
- **영향**: 실제 서버 엔드포인트(`/api/v1/measurement/*`)가 프로젝트에 존재하지 않음. `MEASURE_ERRORS.NETWORK_ERROR`의 "로컬에 저장 후 나중에 동기화됩니다" 문구는 사실과 다름.

---

**[1-C-04] GPS 없는 계측이 서울시청 좌표 + 랜덤 사진으로 서버에 기록됨**

- **위치**: `src/app/api/catch/route.ts:31-32`
- **코드**:
  ```js
  const photo = b.photoUrl || `https://picsum.photos/seed/mycatch-${Date.now()}/800/800`;
  const lat = b.lat ?? 37.5326, lng = b.lng ?? 126.9905;
  ```
- **문제**: 위치 권한 거부·실내·타임아웃(3초, `AutoTagService`) 시 `lat=null,lng=null`으로 호출하면 서버가 서울시청 좌표로 대체해 `FishingPoint`를 생성.
- **영향**: 지도·포인트 통계·낚시단 포인트 공유에 존재하지 않는 조황 데이터 누적. 사진 업로드 실패 시 무관한 랜덤 이미지(picsum)가 계측 사진으로 저장. 되돌리기 어려운 데이터 무결성 훼손.

---

### 🟠 Major

**[1-M-01] 오버레이 캔버스와 비디오의 fit 불일치 — 인식 표시가 실제 물고기 위치와 어긋남**

- **위치**: `src/components/LiveScanCamera.tsx:715-720`
- **문제**: 비디오는 `objectFit:"cover"`인데 오버레이 `<canvas>`는 기본값 `fill`(늘리기). 캔버스 내부 해상도(1920×1080)와 표시 박스(390×844)의 좌표계가 맞지 않아 입낚볼 원·머리/꼬리 점·측정선이 찌그러진 채 엉뚱한 위치에 그려짐.

---

**[1-M-02] IP 기준 30회/분 제한이 2초 폴링과 충돌**

- **위치**: `src/app/api/measure/scan/route.ts:73-76`, `src/components/LiveScanCamera.tsx:51`
- **문제**: 2초 폴링 기준 최대 31회/분으로 이미 상한 초과. 모바일 캐리어 NAT/사내망에서 여러 사용자가 한 버킷 공유. 초과 시 429가 "판정 불가"로 집계되어 "물고기를 찾을 수 없습니다"로 오안내. `x-forwarded-for` 헤더 위조로 제한 우회 가능.

---

**[1-M-03] 무게 추정식이 2벌로 공존 — 같은 물고기가 화면마다 2배 이상 차이**

- **위치**: `src/utils/MeasurementCalculator.js:31-44` vs `src/lib/weightEstimation.ts:22`
- **문제**: 측정 결과 화면은 `MeasurementCalculator.estimateWeight`(배스 `a=0.0085, b=2.98`), 서버/피드/기록 목록은 `weightEstimation.resolveWeightG`(배스 `a=0.00000985, b=3.16`×1000) 사용.
- **영향**: 40cm 배스 기준 전자 ≈ **485g**, 후자 ≈ **1,130g** (2.3배 차이). 계측일지와 피드 카드에 다른 무게가 표시됨.

---

**[1-M-04] 수동 보정 후에도 신뢰도 등급이 "정밀 측정" 그대로**

- **위치**: `src/app/measure/page.tsx:351-364`, `src/app/api/tournaments/[id]/entries/route.ts:24`
- **문제**: 머리/꼬리 좌표를 수동으로 이동해도 `ball.confidence`/`grade`는 AI 최초 값 유지. 임의로 늘린 길이가 "정밀 측정(HIGH)" 배지를 달고 대회 제출까지 가능. `tamperFlag`는 API에서 항상 `false` 하드코딩이라 조작 탐지 장치가 전혀 없음.

---

**[1-M-05] 업로드 이미지 크기 검증 부재**

- **위치**: `src/app/api/measure/scan/route.ts:85-88`
- **문제**: `imageBase64`는 `data:image` 접두사만 확인하고 길이 상한 없음. 수십 MB 문자열을 그대로 OpenAI로 전달 시도 → 메모리 급증 및 API 비용 증가.

---

**[1-M-06] 모든 실패를 단일 문구로 뭉갬**

- **위치**: `src/app/measure/page.tsx:253-255`
- **문제**: `catch { scanFailToChoice(); }` 하나로 타임아웃·401·429·AI 오류·파싱 실패·no-fish를 모두 "자동 측정이 어려운 사진이에요"로 처리. 서버는 `reason`(`timeout|ai-error|rate-limited|no-fish|incomplete`)을 내려주는데 클라이언트가 `no-ball` 외에는 사용하지 않음.

---

**[1-M-07] 클라이언트/서버 타임아웃이 역전**

- **위치**: `src/components/LiveScanCamera.tsx:51`, `src/app/api/measure/scan/route.ts:117`
- **문제**: 클라이언트 `REQ_TIMEOUT_MS=9000`, 서버 `AbortSignal.timeout(10000)`. 주석에 "서버 타임아웃을 클라이언트보다 짧게"라고 명시하지만 실제 값이 반대. GPT 응답이 9~10초 걸리는 정상 케이스에서 항상 클라이언트 실패로 처리됨.

---

### 🟡 Minor

- **[1-m-01]** `src/app/measure/page.tsx:121-131`: `engines()`에서 `new BallDetector()`, `new FishDetector()`를 생성하지만 이후 어디서도 사용하지 않음. 미사용 로컬 CV 엔진 2개(총 431줄)가 번들에 포함됨.
- **[1-m-02]** `src/components/LiveScanCamera.tsx:16`: 사용되지 않는 682줄짜리 `LiveMeasureCamera.tsx` 전체를 import.
- **[1-m-03]** `src/app/measure/page.tsx:1198`: `import { createPortal }` 선언이 파일 중간에 위치 (린트/가독성 문제).
- **[1-m-04]** `src/app/measure/page.tsx:309-312`: `ball`이 없을 때 임의 객체를 만들어 넣으면 `AROverlay._drawResultCard`가 `"null cm"`을 그림.
- **[1-m-05]** `src/constants/errorMessages.js:20-33`: 금지체장 테이블이 불완전(참돔 `minLegalSize: 0`, 일부 지자체 기준 누락). 방생 안내가 법적 기준과 어긋날 수 있음.

---

## 2. 상품 판매/구매 로직 (2순위)

### 🔴 Critical

**[2-C-01] 결제가 `Math.random() < 0.9` mock인데 주문이 `PAID`로 저장됨**

- **위치**: `src/components/shop/ProductPurchaseBar.tsx:158-190`, `src/app/api/me/orders/route.ts:88-99`
- **문제**: 카드번호·유효기간·CVC를 입력받아 1.5초 대기 후 90% 확률로 성공 처리 → `/api/me/orders`에 `status:'PAID'`로 저장 및 재고 차감. 주문 POST 실패(재고 부족 400 등)를 `catch {}`로 삼켜 화면은 결제 성공인데 서버에는 주문 없음 상태가 됨. (실제 카드번호를 입력받는 UI 자체도 위험)

---

**[2-C-02] 입낚볼 주문 경로가 2개로 갈라져 서로 다른 테이블에 기록**

- **위치**: `src/components/IpnakBallPurchase.tsx:236`, `src/app/api/ipnak-ball/orders/route.ts:60-72`, `src/app/shop/ipnak-ball/page.tsx:65`, `src/app/api/shop/ipnak-ball/order/route.ts`
- **문제**:
  - 메인 구매 바텀시트 → `POST /api/ipnak-ball/orders` → **`BallOrder`** 테이블 (`status:"PAID"`, `paymentKey: "TEST_..."`)
  - `/shop/ipnak-ball` 페이지 → `POST /api/shop/ipnak-ball/order` → **`IpnakBallOrder`** 테이블
- **영향**: 관리자 화면은 `BallOrder`만 조회. `IpnakBallOrder`를 읽는 `AdminIpnakBallOrdersTab.tsx`는 어디서도 import되지 않는 죽은 컴포넌트. `/shop/ipnak-ball`에서 들어온 주문은 **관리자가 볼 수도, 상태를 변경할 수도 없음** (결제·재고만 차감되고 배송 처리 불가).

---

**[2-C-03] 구매자가 입낚볼 주문 내역을 볼 수 없음**

- **위치**: `src/app/me/orders/page.tsx:18-42`
- **문제**: `/me/orders`는 raw `Order` 테이블(일반 상품)만 조회. `BallOrder`/`IpnakBallOrder`를 보여주는 사용자 화면이 프로젝트 전체에 없음.
- **영향**: 결제·배송이 진행돼도 구매자는 자기 주문을 조회할 수 없음.

---

### 🟠 Major

**[2-M-01] 중고거래에 "구매자"와 "거래 완료" 개념이 데이터 모델에 없음**

- **위치**: `prisma/schema.prisma:686-705`, `src/app/market/purchases/page.tsx:23-34`
- **문제**: `MarketListing`에 `status`(SELLING/RESERVED/SOLD)만 있고 `buyerId`·거래 확정 시각이 없음. `/market/purchases`(구매내역)는 실제로 `MarketChat` 목록(=말 걸어본 상품)을 보여줄 뿐. 구매 요청/수락/완료 확인 프로세스 부재.

---

**[2-M-02] 판매글 수정 API는 있는데 수정 UI가 없음**

- **위치**: `src/app/api/market/[id]/route.ts:11-35`
- **문제**: `PATCH`는 title/price/description/category/condition/tradeMethod를 모두 받지만 클라이언트에서 status 외의 필드를 보내는 곳이 없음. `/market/new`에도 편집 모드 없어 사용자는 삭제 후 재등록만 가능. 이미지 교체 경로도 없음.

---

**[2-M-03] 진행 중 거래·채팅 보호 없는 삭제**

- **위치**: `src/app/api/market/[id]/route.ts:40-53`
- **문제**: `SOLD` 상태나 진행 중 채팅이 있어도 즉시 삭제 가능. `MarketChat`/`MarketMessage`가 `onDelete: Cascade`로 구매자 쪽 대화 기록까지 통째로 삭제됨(알림 없음). 분쟁 시 근거가 남지 않음.

---

**[2-M-04] 판매글 목록 무제한 조회 — 페이지네이션 없음**

- **위치**: `src/app/api/market/route.ts:31-38`, `src/app/api/products/route.ts:5`
- **문제**: `findMany`에 `take`/`skip` 없이 전체 판매글 + 이미지 + `_count` 집계를 한 번에 반환. 데이터가 쌓일수록 응답 지연이 선형 증가.

---

**[2-M-05] 배송지 소유권 미검증**

- **위치**: `src/app/api/me/orders/route.ts:53-56`
- **문제**: `shippingAddressId`를 해당 주소가 요청자 것인지 확인하지 않고 저장. (`/api/shop/ipnak-ball/order`는 `WHERE id=? AND userId=?`로 올바르게 검증하고 있어 일관성도 깨져 있음)

---

**[2-M-06] 재고 0 = "무제한"이라는 위험한 규칙**

- **위치**: `src/app/api/me/orders/route.ts:64-71`
- **문제**: `if (currentStock > 0 && currentStock < qty)`로 판정하여 **재고 0인 상품은 품절이 아니라 무한 판매됨**. 관리자 상품 폼의 기본값도 0이라 대부분의 상품이 품절되지 않음.

---

### 🟡 Minor

- **[2-m-01]** `src/app/market/[id]/page.tsx:38-44`: `viewCount`를 페이지 렌더마다 증가시킴. Next.js 프리페치·판매자 본인 조회만으로 조회수가 부풀려짐.
- **[2-m-02]** `src/app/api/market/route.ts:24`: 제목만 검색하고 설명/카테고리는 대상 외. SQLite `contains`는 대소문자 처리 한계 있음.
- **[2-m-03]** `src/lib/ipnakProduct.ts:73-95`: 요청 처리 경로에서 `ALTER TABLE`을 시도하고 실패를 무시함. 개발 로그가 `duplicate column name` 에러로 오염됨. 마이그레이션으로 옮겨야 함.
- **[2-m-04]** `src/components/MockPayModal.tsx:26-29` + `src/app/api/points/charge/route.ts:16`: 포인트 충전 UI는 "준비 중" 토스트만 띄우지만 **API는 열려 있어** 직접 호출 시 금액 검증 없이 원하는 만큼 포인트 발행 가능. (Major에 가까운 항목)

---

## 3. 계정별 피드 권한 (3순위)

### 🔴 Critical

**[3-C-01] 워킹 피드 유료 잠금이 클라이언트 오버레이에 불과 — 페이월 우회 가능**

- **위치**: `src/lib/queries.ts:31-63`, `src/components/FeedCard.tsx:74`
- **문제**: `toFeedPost`는 `walkingLocked` 플래그만 추가할 뿐 `body`(GPS 동선 좌표·거리·어획 마커)와 `images`(사진 URL)를 그대로 직렬화. 개발자도구 네트워크 탭으로 잠금 콘텐츠를 볼 수 있음. 잠금은 `FeedCard.tsx:482`의 오버레이 div뿐.
- **영향**: 유료 기능의 수익 근거가 무너지고 **타인의 낚시 포인트 GPS가 무단 노출됨**.
- **참고**: 현재 `pointsEnabled()`가 기본적으로 false라(`src/lib/settings.ts`) 워킹 피드 유료화 자체가 비활성 상태임 (의도 여부 확인 필요).

---

**[3-C-02] 비공개 글의 댓글이 비로그인 사용자에게 공개**

- **위치**: `src/app/api/posts/[id]/comments/route.ts:7-15`
- **문제**: GET에 인증·공개범위 검사 전혀 없음. `visibility: PRIVATE`/`FOLLOWERS` 글이나 `hidden: true` 글이라도 postId만 알면 댓글 전체(작성자 닉네임·아바타 포함) 조회 가능. POST(19행)도 대상 글의 공개범위·hidden 여부를 확인하지 않아 비공개 글에 댓글 달기 가능.

---

### 🟠 Major

**[3-M-01] 본인 게시물 수정·삭제 기능 자체가 없음**

- **위치**: `src/app/api` 전체
- **문제**: 게시글/댓글의 사용자 수정·삭제 엔드포인트(`PUT`/`PATCH`/`DELETE` for posts/comments) 부재. 삭제 가능한 것은 관리자의 `POST_HIDE`/`COMMENT_HIDE`(숨김일 뿐 삭제 아님)와 스마트피싱 기록 삭제뿐.
- **영향**: 개인정보·명예훼손 삭제 요구 대응 불가 (법적 리스크).

---

**[3-M-02] 좋아요·북마크·신고·공유에 대상 글 접근권 검사 없음**

- **위치**: `src/app/api/posts/[id]/like/route.ts:7`, `bookmark/route.ts:7`, `report/route.ts:7`, `share/route.ts:7`
- **문제**: postId만 알면 비공개/숨김 글에도 좋아요를 눌러 작성자에게 알림을 보낼 수 있음. `share`는 비로그인도 허용이라 익명 이벤트 스팸 가능.

---

**[3-M-03] 피드 팔로잉 필터에 SQLite 파라미터 한도 위험**

- **위치**: `src/lib/queries.ts:110-152`
- **문제**: `prisma.follow.findMany`로 팔로잉 ID 전체를 메모리에 올려 `authorId: { in: [...] }`로 넘김. 팔로잉 수가 많아지면 SQLite 파라미터 한도(999)에 걸려 쿼리 실패 가능. 실패 시 `catch`에서 `kind` 없는 재조회로 폴백해 의도치 않게 다른 종류의 글이 섞여 나올 수 있음.

---

**[3-M-04] 낚시단 피드 테이블을 런타임에 CREATE TABLE로 생성**

- **위치**: `src/lib/groupPosts.ts:44-80`
- **문제**: `GroupPost`/`GroupComment`/`GroupPostLike`는 schema.prisma에도 있는데 코드가 raw SQL로 별도 생성. 스키마 드리프트(컬럼 불일치) 시 조용히 다른 테이블을 사용하게 됨.

---

### 🟡 Minor

- **[3-m-01]** `src/middleware.ts:3-9`: 미들웨어가 `/`만 매칭해 리다이렉트할 뿐 인증 게이트 역할을 전혀 하지 않음. 모든 보호가 각 라우트의 개별 `requireUser()`에 의존하여 새 API 추가 시 누락 위험 큼 (실제로 댓글 GET이 그 사례).
- **[3-m-02]** `src/lib/queries.ts:70-81`: `pointsEnabled()`가 false면 전부 잠금 해제. 현재 기본 설정이 `points_enabled: "false"`라 워킹 피드 유료화는 현재 전면 비활성 상태 (의도된 것인지 확인 필요).
- **[3-m-03]** `src/app/api/posts/route.ts:60-74`: 글 작성 실패 시 `kind/title/body/boardCategory`를 제거하고 재시도하는 레거시 폴백이 있어 DB 문제 시 제목·본문 없는 유령 글이 생길 수 있음.

---

## 4. 전반적 코드 품질 (4순위)

### 🔴 Critical

**[4-C-01] 시크릿 키가 작업트리에 평문 저장**

- **위치**: `.env`
- **문제**: `SESSION_SECRET`, 소셜 로그인 시크릿, `PAYMENT_API_KEY`, `NAVER_SEARCH_CLIENT_SECRET` 등이 평문으로 존재. `_backup.bat`·zip이 함께 있는 구조라 유출 위험 큼.
- **영향**: `SESSION_SECRET`은 `aiCredentials.ts:12-21`에서 OpenAI 키 등의 **AES-256-GCM 마스터 키 파생에 사용**되므로 유출 시 저장된 모든 외부 자격증명이 복호화됨.

---

**[4-C-02] 세션 쿠키에 CSRF 방어 없음 + 비밀번호 변경 후 세션 무효화 부재**

- **위치**: `src/lib/auth.ts:55-65`, `/api/me/password`, `src/app/api/auth/reset-password/route.ts:44`
- **문제**: `sameSite: "lax"`만으로 상태 변경 API 보호. 토큰 기반 CSRF 방어나 Origin 검증 없음. 비밀번호 변경(`/api/me/password`) 시 기존 세션을 무효화하지 않음.
- **영향**: 계정 탈취 후 비밀번호를 바꿔도 피해자의 다른 세션이 살아있고, 반대로 피해자가 비밀번호를 바꿔도 공격자 세션 유지됨.

---

### 🟠 Major

**[4-M-01] 파일 업로드 MIME 검증 부재 + `public/uploads` 저장으로 배포 시 유실**

- **위치**: `src/app/api/upload/route.ts:31-45`, `src/app/api/admin/upload/route.ts:26`
- **문제**: 확장자만 보고 저장하며 매직바이트/실제 MIME 검증 없음. `.heic`는 변환 없이 `.jpg`로 이름만 변경하여 브라우저에서 깨짐. 저장 위치가 `public/uploads`라 Vercel 등 읽기전용 FS에서는 런타임 실패, 컨테이너 재배포 시 업로드 전부 사라짐. 관리자 업로드는 8MB 제한도 없음.

---

**[4-M-02] Rate limit이 프로세스 메모리 기반 — 다중 인스턴스에서 무효**

- **위치**: `src/lib/rateLimit.ts:5`
- **문제**: 프로세스 메모리 Map 기반이라 다중 인스턴스에서 무효. 만료 엔트리를 청소하지 않아 메모리 누수 가능성 있음.

---

**[4-M-03] 광범위한 `catch {}` 무음 처리 — DB 장애가 "글이 없음"으로 표시됨**

- **위치**: `src/lib/queries.ts:229,281,298,395,430`, `src/app/api/posts/feed/route.ts:16`, `src/app/api/trips/[id]/route.ts:46` 등
- **문제**: 예외를 삼키고 빈 배열/`{posts: []}`를 반환. DB 장애가 "글이 없음"으로 표시되어 장애 인지 불가. 서버 로깅도 없음.

---

**[4-M-04] 타입 안전성 붕괴 — API 전반이 `any`로 처리됨**

- **위치**: `src/app/api/catch/route.ts:26`, `src/app/api/posts/route.ts:28`, `src/app/api/admin/action/route.ts:33`, `src/lib/queries.ts:294,304` 등
- **문제**: `strict: true`인데 API 라우트 대부분이 `const b = await req.json()` 후 `any`로 처리. 런타임 검증은 회원가입(zod)에만 있음. `prisma.post as any`, `(prisma as any).curationFeature` 등으로 스키마 변경 시 컴파일 타임에 잡히지 않음.

---

**[4-M-05] 관리자 주문 조회가 LIMIT 200 고정 — 이후 주문은 영구히 사라짐**

- **위치**: `src/app/api/admin/ipnak-ball/orders/route.ts:26-40`
- **문제**: `LIMIT 200` 고정에 페이지네이션 없음. 200건 초과 시 과거 주문이 화면에서 사라져 취소·환불 처리 불가.

---

### 🟡 Minor

- **[4-m-01]** `src/app/api/admin/action/route.ts:29-31`: 30개 이상의 이질적 관리자 액션이 단일 `POST` 라우트의 거대한 switch에 몰려 있음(300행+). 액션별 입력 검증 수준이 제각각.
- **[4-m-02]** `src/app/api/geocode/route.ts:5-20`: 인증·레이트리밋 없이 Nominatim으로 프록시. 외부 서비스 이용약관(초당 1회) 위반 및 오픈 프록시로 악용 가능.
- **[4-m-03]** `prisma/schema.prisma:99-107`: `LinkedBall`의 유니크 제약이 `(userId, ballId)`라 동일한 입낚볼 ID를 여러 계정이 동시에 등록 가능. 볼 소유권 기반 인증에 쓰려면 `ballId` 전역 유니크 필요.
- **[4-m-04]** `src/app/measure/page.tsx:604-616`, `src/components/LiveScanCamera.tsx:684-696`: `rotate(-90deg)` + `100dvh/100vw` CSS 회전 트릭. 접근성, iOS Safari 주소창 높이 변화, 회전 중 터치 좌표 어긋남 등 유지보수 리스크.
- **[4-m-05]** 테스트용 파일(`src/components/FeedHeader.SYNCTEST.tsx`), `.next/` 빌드 산출물, `tsconfig.tsbuildinfo`(228KB), `fishing-icons.zip`, `*.log`, `.group-data/`(JSON 저장소 잔재) 등이 저장소에 포함되어 있음.

---

## 권장 조치 순서

1. **`/api/measure/scan`의 `no-ai-key`/`rate-limited`/`timeout`을 클라이언트가 구분해 안내** + 측정 진입 시 `/api/ai/status`로 사전 확인 (AI 계측 기능 정상화의 전제)
2. **계측 기록을 서버 저장으로 전환** — `CatchRecord`에 필요한 컬럼(`bodyWidth`, `measuredLengthCm`, `confidence`)이 이미 있으므로 `DiarySheet`를 서버 조회로 변경 시 계정 혼입/유실 동시 해결
3. **`/api/catch`의 좌표·사진 기본값 제거** (null 허용) — 이미 저장된 서울시청 좌표 데이터 정리 필요
4. **워킹 피드 잠금을 서버에서 적용** — `toFeedPost`에서 `walkingLocked === true`면 `body`/`images`/좌표를 응답에서 제거
5. **결제 mock 경로 차단** — `/api/points/charge`와 `ProductPurchaseBar`의 랜덤 승인을 실 PG 연동 전까지 비활성화. 주문 status를 `PENDING`으로 변경
6. **입낚볼 주문 경로 통일** — `BallOrder`/`IpnakBallOrder` 중 하나로 일원화. 구매자 주문 내역 화면 신설
7. **무게 산출식 통일** — `lib/weightEstimation.ts` 하나로, `MeasurementCalculator.estimateWeight` 제거
8. **본인 게시물 수정/삭제 API 신설** + 댓글 GET/POST에 공개범위 검사 추가
9. **`.env`를 저장소에서 제거** (secrets 로테이션 필요), `.gitignore` 확인
10. 비밀번호 변경 시 기존 세션 전체 무효화 추가
