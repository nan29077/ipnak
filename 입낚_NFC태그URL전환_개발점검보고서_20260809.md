# 입낚 NFC 태그 URL 전환 — 개발 · 점검 보고서

- 작성일: 2026-08-09
- 브랜치: `sub` (커밋/푸시 미실행 — 지시 대기)
- 목적: NFC 태그를 URL 방식으로 전환해 아이폰에서도 "태그 → 계측"이 한 번에 이어지도록 함

---

## 1. 개발 결과 요약

| # | 요구사항 | 상태 |
|---|---|---|
| 1 | NFC 태그 URL 방식 전환 (볼·키링) | 완료 |
| 2 | `/ball?id=XXX`, `/keyring?id=XXX` 라우트 + 4분기 | 완료 |
| 3 | 아이폰 감지 → 연동 화면 안내 변경 (수동입력 토글로 접기) | 완료 |
| 4 | 아이폰 최초 계측 태그 시 1회성 가이드 팝업 | 완료 |
| 5 | 볼·키링 동일 적용 | 완료 |
| 6 | ID 형식 확정 (BALL-XXXXXX / KRING-XXXXXX) + 구형 IPNK 하위호환 | 완료 |
| 7 | 관리자 ID 관리 탭에 NFC 태그 프로그래밍 안내 섹션 | 완료 |

---

## 2. 태그 URL 규격 (확정)

```
볼   : https://ipnak.kr/ball?id=BALL-000001      (BALL  + 숫자 6자리)
키링 : https://ipnak.kr/keyring?id=KRING-000001  (KRING + 숫자 6자리)
```

예시 ID·형식 문구는 `src/lib/nfcTag.ts` 의 `ID_SAMPLE` / `ID_FORMAT_LABEL` 한 곳에서만 관리합니다.
(관리자 placeholder, 사용자 수동입력 placeholder, 연동 방법 시트 샘플 코드가 전부 이 값을 참조)

- 기준 도메인은 `NEXT_PUBLIC_TAG_BASE_URL` 로 변경 가능 (기본 `https://ipnak.kr`)
- 관리자 화면(볼 ID 관리 / 키링 ID 관리)에서 **ID 옆 복사 아이콘**으로 태그 URL을 그대로 복사할 수 있고,
  하단 가이드 문구도 "텍스트 쓰기" → "**URL(링크) 레코드 쓰기**"로 바뀜

### 하위 호환 (중요)
기존에 이미 발행한 텍스트 태그를 **버리지 않아도 됩니다.** 파서가 아래를 모두 인식합니다.

| 태그 내용 | 인식 결과 |
|---|---|
| `BALL-000001` / `KRING-000001` (신규 텍스트) | 접두어로 볼·키링 구분 + ID |
| `IPNK-000001` / `IPNK-KR-000001` (**구형 텍스트**) | 접두어로 볼·키링 구분 + ID — **안드로이드 기존 태그 그대로 동작** |
| NDEF Text 레코드(상태바이트+언어코드 포함) | ID만 추출 |
| `https://ipnak.kr/ball?id=BALL-000001` | 볼 + ID |
| `https://ipnak.kr/keyring?id=KRING-000001` | 키링 + ID |
| `https://ipnak.com/ball/IPNK-000001` (구 경로형) | 볼 + ID |
| `ipnak://ball/XXX`, `ipnak://keyring?id=XXX` | 볼/키링 + ID |
| 외부 도메인 URL, 형식 위반 ID | **거부(null)** |

ID 접두어로 상품 종류를 유추하는 `inferKindFromId()` 를 추가했습니다.
`BALL-` / `KRING-` 은 물론 구형 `IPNK-` / `IPNK-KR-` 도 올바르게 구분합니다.
(구형 키링 `IPNK-KR-` 이 구형 볼 `IPNK-` 보다 먼저 검사되도록 순서를 고정)

**이미 배포된 IPNK 태그는 재프로그래밍 없이 계속 쓸 수 있습니다.**
다만 아이폰에서는 텍스트 태그에 배너가 뜨지 않으므로, 아이폰 지원을 위해서는 URL 재프로그래밍이 필요합니다.
이 내용은 관리자 화면 안내에도 명시했습니다.

---

## 3. `/ball?id=XXX` 분기 로직 (키링 동일)

```
URL 진입
 ├─ id 없음/형식 위반           → /measure
 ├─ 레지스트리 미등록·비활성     → /ball-not-found?ballId=XXX[&type=keyring]
 ├─ 비로그인                    → /login?redirect=/ball?id=XXX
 └─ 로그인
      ├─ 내 계정에 등록됨        → /measure?ballId=XXX&fromTag=1  (즉시 계측, ID 자동 선택)
      ├─ 아무에게도 미등록       → "이 입낚볼을 등록하시겠습니까?" → 등록 → 계측 화면
      └─ 타인 소유              → "다른 사용자의 볼입니다" 안내
```

로그인 후 복귀는 이메일 로그인·소셜 로그인 모두 지원합니다.
(소셜은 서버 리디렉션이라 5분짜리 임시 쿠키로 복귀 경로를 전달 — 내부 경로만 통과시키는 검증 포함)

---

## 4. 변경 파일

### 신규 (10)
| 파일 | 역할 |
|---|---|
| `src/lib/nfcTag.ts` | 태그 URL 생성 + 파서(하위호환 포함) |
| `src/lib/device.ts` | iOS 판별 (Capacitor 플랫폼 + UA + iPadOS 위장 대응) |
| `src/lib/tagLanding.ts` | 태그 랜딩 4분기 서버 판별 (볼/키링) |
| `src/lib/safeRedirect.ts` | 로그인 복귀 경로 검증(오픈 리다이렉트 차단) |
| `src/app/ball/page.tsx` | `/ball?id=XXX` 라우트 |
| `src/app/keyring/page.tsx` | `/keyring?id=XXX` 라우트 |
| `src/components/TagLandingView.tsx` | 등록 확인 / 타인 소유 안내 화면 |
| `src/components/IphoneTagGuideModal.tsx` | 아이폰 1회성 가이드 팝업 |
| `src/components/admin/TagUrlCopyButton.tsx` | 관리자 태그 URL 복사 버튼 |
| `src/components/admin/NfcTagProgrammingGuide.tsx` | 관리자 NFC 태그 프로그래밍 안내 (아코디언, 볼·키링 공용) |

### 수정 (16)
| 파일 | 변경 내용 |
|---|---|
| `src/services/NfcService.ts` | URL 태그 해석 추가, `readTag()` 신설 (`readBallId()` 시그니처 유지) |
| `src/hooks/useNfc.ts` | `decodeNdefText`가 공용 파서를 쓰도록 교체 (**안드로이드 흐름 로직 자체는 그대로**) |
| `src/app/measure/page.tsx` | `ballId`/`keyringId`/`fromTag` 파라미터 처리, 태그 선택 배너, 아이폰 가이드 연동 |
| `src/components/BallLinkSection.tsx` | 아이폰 전용 안내 + `직접 입력하기` 토글, 아이폰 가이드 시트 문구 개편 |
| `src/app/login/page.tsx` | `?redirect=` 지원 + 태그 진입 안내 배너 |
| `src/app/api/auth/kakao/callback/route.ts` | 로그인 후 복귀 경로 반영 |
| `src/app/api/auth/naver/callback/route.ts` | 로그인 후 복귀 경로 반영 |
| `src/app/ball-not-found/page.tsx` | 키링 대응(`?type=keyring`) + 스캔한 ID 표시 |
| `src/components/admin/IpnakBallRegistryTab.tsx` | 태그 URL 복사 버튼 + NFC 프로그래밍 안내 아코디언 + placeholder `BALL-000001` |
| `src/components/admin/IpnakKeyringRegistryTab.tsx` | 동일 (placeholder `KRING-000001`) |
| `src/components/BallManualInput.tsx` | placeholder 를 신규 형식으로 교체 |
| `src/app/ball/[ballId]/page.tsx` | 주석만 정리 (구 경로형은 QR·구형 태그 호환용으로 유지) |
| `src/hooks/useDeepLink.ts` | 허용 호스트에 `ipnak.kr` 추가 |
| `capacitor.config.ts` | intentFilter에 `/keyring` 및 `ipnak.kr` 추가 |
| `.env.example` | `NEXT_PUBLIC_TAG_BASE_URL` 문서화 |

---

## 5. 점검 결과

### 5-1. 정적 검증
| 항목 | 결과 |
|---|---|
| `tsc --noEmit` | **오류 0건** |
| `next build` | **Compiled successfully / 104 페이지 생성 성공** |
| 라우트 충돌 | `/ball`, `/ball/[ballId]`, `/keyring` 모두 독립 등록 — 충돌 없음 |
| 신규 ESLint 경고 | 없음 (기존 경고만 유지) |

### 5-2. 태그 파서 단위 검증 — **27/27 통과** (형식 확정 후 재실행)
신규 URL 태그 6종, 구형 텍스트 태그 8종(NDEF 언어코드 변형 포함), 신규 텍스트 태그 3종,
거부 케이스 4종, 접두어 종류 유추 6종.
구형 `IPNK-000001` → `ball`, `IPNK-KR-000001` → `keyring` 판별까지 확인.

### 5-3. `/ball` 4분기 (로그인 계정: angler@ipnak.test)
| 케이스 | 결과 |
|---|---|
| 내 볼 `IPNK-TSTMINE` | → `/measure?ballId=IPNK-TSTMINE&fromTag=1` |
| 미등록 `IPNK-TSTNEW` | → "이 입낚볼을 등록하시겠습니까?" 렌더 |
| 타인 소유 `IPNK-TSTOTHER` | → "다른 사용자의 볼입니다" 렌더 |
| 비활성 `IPNK-TSTOFF` | → `/ball-not-found?ballId=IPNK-TSTOFF` |
| 레지스트리 없음 `NOPE-999` | → `/ball-not-found` |
| id 파라미터 없음 | → `/measure` |
| 형식 위반 id (` //`) | → `/measure` |
| **비로그인** | → `/login?redirect=%2Fball%3Fid%3DIPNK-TSTNEW` |

### 5-4. `/keyring` 4분기 — 볼과 동일하게 4건 전부 정상

### 5-4-1. 형식 확정 후 재검증 (신규 · 구형 혼재 상태)
| 케이스 | 결과 |
|---|---|
| 신규 볼 `/ball?id=BALL-000001` | 등록 확인 화면 → 등록 → `/measure?ballId=BALL-000001&fromTag=1` |
| 신규 키링 `/keyring?id=KRING-000001` | 등록 확인 화면 → 등록 → `/measure?keyringId=KRING-000001&fromTag=1` |
| **구형 볼** `/ball?id=IPNK-000009` | 정상 인식 (등록 확인 화면) |
| **구형 키링** `/keyring?id=IPNK-KR-000009` | 정상 인식 (등록 확인 화면) |
| 레지스트리 없음 `/ball?id=BALL-999999` | `/ball-not-found?ballId=BALL-999999` |

레지스트리에 신규(BALL/KRING)와 구형(IPNK) ID를 **섞어 둔 상태**에서 모두 정상 동작했고,
관리자 복사 버튼도 각 ID에 맞는 URL(`/ball?id=IPNK-000009`, `/keyring?id=IPNK-KR-000009`)을 생성했습니다.

### 5-5. 등록 API 방어
| 요청 | 결과 |
|---|---|
| 미등록 ID 등록 | 201 생성 |
| 타인 소유 ID 등록 | **409 거부** |
| 비활성 ID 등록 | **400 거부** |
| 키링도 동일 | 201 / 409 확인 |

### 5-6. 실제 브라우저 전 구간 시뮬레이션 (모바일 375×812)
1. 비로그인 `/ball?id=IPNK-TSTNEW` → 로그인 화면 + "입낚볼 태그를 인식했어요" 배너 ✔
2. 로그인 → **`/ball?id=IPNK-TSTNEW` 으로 자동 복귀** ✔
3. "등록하기" → 토스트 + `/measure?ballId=IPNK-TSTNEW&fromTag=1` 이동 ✔
4. 계측 화면 상단에 "입낚볼 IPNK-TSTNEW 선택됨 — 바로 측정할 수 있어요" 배너 ✔
5. AI 카메라 자동 오픈 ✔ / 콘솔 에러 **0건** ✔
6. 태그 없이 `/measure` 진입 시 배너 미노출 ✔

### 5-7. 아이폰 UI (iOS 판별 강제 후 실제 렌더 확인 → 검증 후 원복 완료)
- 볼·키링 연동 카드 모두 "**볼(키링)을 아이폰에 가져다 대세요**" + 3단계 안내로 교체 ✔
- 수동 입력은 "**직접 입력하기**" 토글 안으로 접힘 → 펼치면 기존 입력폼 정상 동작, `font-size: 16px` 유지(iOS 확대 방지) ✔
- 1회성 가이드 팝업: `fromTag=1` 첫 진입 시 노출 → "확인했어요" → **localStorage 저장 + 카메라 자동 오픈** ✔
- **재진입 시 팝업 미노출, 카메라만 바로 오픈** ✔
- 팝업 노출 중 배경 스크롤 잠금 / 닫으면 해제 ✔

### 5-8. 안드로이드·데스크톱 회귀
| 환경 | 결과 |
|---|---|
| NFC 지원(안드로이드) | "볼에 NFC 태그하기" / "키링에 NFC 태그하기" 버튼 **기존 그대로** |
| NFC 미지원 + 비-아이폰(PC 브라우저) | 기존 수동 입력 UI **그대로** |
| 마이페이지 볼·키링 관리 | **미변경** (요청 범위가 AI 측정 화면이라 손대지 않음) |

### 5-9. 기존 기능 무결성 — 23개 페이지 스모크 테스트
`/home /feed /diary /me /settings /log /map /measure /explore /groups /tournaments /shop /market /landing /login /signup /ruler /trip /walking /reservations /referral /ball-not-found` → **전부 200**, `/` → 307(기존 랜딩 리디렉션 정상).
관리자 `/admin`, `/admin/ipnak-ball` → 200, 볼/키링 ID 관리 탭에서 태그 URL 복사 버튼 정상.

### 5-9-1. 관리자 NFC 태그 프로그래밍 안내 (신규)
`입낚볼/입낚키링 관리 → 볼 ID 관리 / 키링 ID 관리` 탭의 목록 아래에 **접이식 아코디언**으로 배치.
기본은 접힌 상태라 기존 표 화면을 가리지 않습니다.

| 확인 항목 | 결과 |
|---|---|
| 기본 접힘 / 클릭 시 펼침 (`aria-expanded` 토글) | ✔ |
| NFC Tools 앱 안내 (iOS·Android 무료, iPhone 7 이상) | ✔ |
| 볼·키링 URL 형식 카드 (현재 탭 쪽이 주황색으로 강조) | ✔ |
| 각 형식 카드에 URL 복사 버튼 | ✔ |
| 단계별 입력 방법 6단계 (앱 실행 → WRITE → 레코드 추가/URI → URL 입력 → 태그 접촉 → 완료 확인) | ✔ |
| 구형 IPNK 텍스트 태그 계속 사용 가능 + **URL 재프로그래밍 권장** 안내 | ✔ |
| 쓰기 전 체크리스트 (URI 레코드 여부, `?id=` 포함, 경로, 활성 상태, Lock 주의) | ✔ |
| 볼 탭 헤더 "입낚볼 …", 키링 탭 헤더 "입낚키링 …" 로 각각 표시 | ✔ |

### 5-10. UIUX 점검
- 태그 → 계측까지 **탭 1회**(등록된 볼) / **탭 2회**(신규 등록) — 목표 달성
- 비로그인 진입 시 로그인 화면에 "왜 여기로 왔는지" 설명 배너를 넣어 이탈 방지
- 등록 확인 화면에 볼 ID를 모노스페이스로 크게 노출 → 다른 볼을 잘못 등록하는 실수 방지
- "나중에 하기" 제공 → 강제 등록 아님
- 타인 소유 화면은 빨간색 경고 톤 + 해결 방법(기존 계정에서 해제) 안내
- 아이폰 가이드 팝업은 기존 PC 미지원 팝업과 동일한 디자인 언어 사용 (일관성)
- 관리자 안내는 아코디언이라 평소엔 표를 가리지 않고, 필요할 때만 펼쳐 보게 함
- 관리자 안내에서 **현재 보고 있는 탭(볼/키링)의 URL 형식을 강조색**으로 구분해 오입력 방지
- 사용자·관리자 화면의 예시 ID를 코드 한 곳(`ID_SAMPLE`)에서 관리해 형식이 다시 바뀌어도 문구가 어긋나지 않음

---

## 6. 개발 중 발견해 고친 버그

**NDEF 텍스트 태그를 URL로 오인해 기존 안드로이드 등록이 깨지는 문제**
NDEF Text 레코드의 상태 바이트(예: `0x02` = 언어코드 2바이트)와 URI 레코드의 축약 프리픽스(`0x02` = `https://www.`)가 값이 겹칩니다.
초안 파서는 이를 URI로 단정해 `\x02koIPNK-000002` 같은 **기존 태그를 `https://www.koIPNK-000002` 로 잘못 해석 → 인식 실패**했습니다.
→ 가능한 해석을 후보로 모두 만들어 **실제로 ID가 나오는 첫 후보를 채택**하고, Text 레코드는 상태 바이트의 언어코드 길이 필드로 정확히 잘라내도록 수정. 단위 테스트 24케이스로 회귀 고정.

---

## 7. 배포 전 확인 필요 사항 (코드 밖 작업)

1. **`ipnak.kr` 도메인이 실제로 앱/웹을 서빙해야 합니다.**
   현재 `capacitor.config.ts` 의 `PROD_URL` 은 `https://ipnak.com` 입니다.
   `ipnak.kr` 이 `ipnak.com` 으로 301 리디렉트만 되어도 동작하지만, 리디렉트 시 iOS 유니버설 링크가 앱이 아닌 Safari로 열립니다.
   → 태그 도메인을 `ipnak.com` 으로 쓰시려면 `NEXT_PUBLIC_TAG_BASE_URL=https://ipnak.com` 만 설정하면 됩니다.

2. **iOS 유니버설 링크(앱으로 바로 열기)를 원하면**
   - Xcode → Associated Domains 에 `applinks:ipnak.kr` (및 `applinks:ipnak.com`) 추가
   - 서버 `/.well-known/apple-app-site-association` 배포
   - 미설정 시에도 **Safari 웹으로는 정상 동작**합니다 (기능 자체는 문제 없음).

3. **Android App Links** — `/.well-known/assetlinks.json` 배포 필요 (미설정 시 브라우저로 열림).

4. **`useDeepLink` 훅이 아직 어디에도 마운트돼 있지 않습니다** (이번 작업 이전부터 그런 상태).
   앱 내부에서 딥링크 이벤트를 받아 화면 전환하려면 `AppShell` 등에 1회 마운트가 필요합니다. 이번 범위 밖이라 손대지 않았습니다.

5. **NFC 칩 쓰기 방식 변경 안내** — 생산/발주처에 "텍스트"가 아니라 "**URL(링크)**" 레코드로 써야 한다고 전달 필요.

---

## 8. 참고 / 후속 제안 (이번에 손대지 않은 것)

- **`/ball/[ballId]` (구 QR 경로) 는 소유권 검사 없이 자동 등록합니다.**
  이번에 만든 `/ball?id=` 는 타인 소유를 막지만, 구 경로는 확인 없이 `LinkedBall` 을 생성해 **다른 사람 볼을 중복 등록할 수 있습니다.** (기존부터 있던 문제)
  → 구 경로도 `/ball?id=` 로 합치거나 동일 검사를 넣는 것을 권합니다.
- **마이페이지 볼·키링 관리 섹션**은 아이폰에서도 여전히 "박스에 표시된 아이디를 직접 입력하세요"만 보입니다. 요청 범위(AI 측정 화면)를 지키느라 두었습니다. 통일하려면 동일 컴포넌트 적용만 하면 됩니다.
- **키링 서비스 스위치가 꺼져 있으면**(현재 기본값 OFF) 키링 태그로 진입해도 기준물 모드가 볼로 보정됩니다. 키링 출시 시 관리자에서 스위치를 켜야 정상 동작합니다. (점검 시 임시로 켜서 확인 후 원복 완료)
- 소셜 로그인(카카오/네이버) 복귀 경로는 **코드 경로와 쿠키 파싱 단위 검증까지만** 수행했습니다. 실제 OAuth 왕복은 운영 키가 필요해 미실행입니다.
- 태그 랜딩의 로그인 리디렉션은 Next.js 특성상 앱 셸이 잠깐 보인 뒤 전환됩니다(기존 `/ball/[ballId]` 와 동일 동작).

---

## 9. 점검용 임시 데이터 정리

- 임시 레지스트리/연동 데이터(`IPNK-TST*`, `IPNK-KR-TST*`, `BALL-000001`, `BALL-000002`, `KRING-000001`, `IPNK-000009`, `IPNK-KR-000009`) **전량 삭제 완료**
- 임시로 켰던 `ipnak_keyring_enabled` 설정 행 **삭제 완료** (원래 없던 행)
- iOS 강제 판별 임시 코드 **원복 완료** (`grep TEMP-VERIFY` 결과 0건)
- 검증 빌드 산출물 `.next-verify` 삭제, 검증용 스크립트 삭제
- **커밋/푸시는 하지 않았습니다.**
